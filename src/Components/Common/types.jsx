// types.jsx
//  example to import from other files


// Define the Id type
const Id = String | null;

// Define the Columns type
const Columns = {
    id: Id,
    title: String,
};

export default Columns;